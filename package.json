{
  "name": "agent-web-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Web-based terminal bridge for Claude CLI using node-pty + WebSocket + xterm.js",
  "main": "server.js",
  "bin": {
    "agent-web-bridge": "./server.js"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.22.1",
    "node-pty": "^1.1.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
